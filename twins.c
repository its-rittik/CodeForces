// https://codeforces.com/problemset/problem/160/A
/*#include<stdio.h>
 int mian(){

    int i , j ,n , temp , sum=0 , sum2=0 ;
    int a[n];
    scanf("%d" , &n);
    for(i=0 ; i < n ; i++){
        scanf("%d" ,&a[i]);
    }
    for(i=0 ; i< n  ; i++){
        sum+=a[i];
    }
    for(i=0 ; i<n ; i++){
        for(j=i+1 ; j<n ;j++){
            if(a[i],a[j]){
                temp = a [i];
                a[i] = a[j];
                a[j]= temp;
            }
        }
        sum2+=a[i];
         if(sum2>(sum/2)){
        printf("%d" , i);
    }
    }

 }  */
